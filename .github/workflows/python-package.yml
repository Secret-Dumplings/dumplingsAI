# dumplingsAI 包测试 + lint CI
# 使用 uv 管理依赖；lint 用 ruff；test 用 pytest。
name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true
        # 子模块没有 uv.lock（依赖 lock 在根项目 AI_Company/uv.lock 里），
        # 不能用 cache-dependency-glob: "uv.lock"，否则 setup-uv 会因为找不到文件而失败。
        cache-dependency-glob: "**/pyproject.toml"

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Sync dependencies (含 dev 组)
      run: uv sync --group dev

    - name: Lint with ruff
      run: uv run ruff check .

    - name: Test with pytest
      run: uv run pytest
